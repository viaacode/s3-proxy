# frozen_string_literal: true

source 'https://rubygems.org'

gem 'aws-sdk-s3' # latest version now also works with newer Caringo
gem 'json'
# gem 'puma' # this works ok with minio, but has Expect header issue with Caringo
gem 'progress_bar' # just some eye candy for large uploads/downloads
gem 'puma'
gem 'redis'
gem 'rest-client'
gem 'sidekiq'
gem 'sinatra'
gem 'sinatra-contrib'
gem 'unicorn'

gem 'activesupport' # for hash.from_xml

# for signalling to vrt and signalling new uploads
gem 'bunny'

# for tests
gem 'minitest'
gem 'rack-test'
gem 'rake'
gem 'rubocop'
gem 'test-unit'
gem 'webmock'
